import React, { FC } from "react";

const Main: FC = () => {
    return(
        <header className="header">
            <h1 className="header__title">Good Trip</h1>
        </header>
    );
}

export default Main;